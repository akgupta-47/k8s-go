apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-golang-kubernetes-service"
  labels:
    app: go-pod
spec:
  selector:
    app: go-pod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
